// import React from 'react'

// export default function getTransferLocation () {

//   const apiUrl = 'https://api.test.hotelbeds.com/transfer-cache-api/1.0/locations/countries?fields=ALL&language=ES&offset=1&limit=1000'

//   return fetch(apiUrl).then (res => res.json()).then response => {
//     const {data =[]} = response
//     if (Array.isArray(data)) {
//       const countries = data.map( country => {
//         const {}
//       })
//     }
//   }
// }
